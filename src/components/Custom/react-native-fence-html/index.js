/** @format */

import HTML from 'react-native-render-html';
import * as HTMLStylesModule from 'react-native-render-html/src/HTMLStyles';
import { STYLESETS } from 'react-native-render-html/src/HTMLUtils';

export const HTMLStyles = {
  ...HTMLStylesModule,
  STYLESETS,
};

export default HTML;
